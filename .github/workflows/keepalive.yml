name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *' # Every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -s -o /dev/null -w "%{http_code}" https://kulve.us/api/health || true
